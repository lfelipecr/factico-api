# Variables para Vercel (Production) — proyecto panel + proxy API
# api.factico.net = otro proyecto Vercel (no usar aquí)
# apife.factico.net = este deploy (/v1 → Edge Function)

NEXT_PUBLIC_SUPABASE_URL=https://rabcundgpzfzwnntjuxu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=pegar-desde-supabase-dashboard-settings-api

# Llamadas admin del panel (provision, certificado, etc.)
NEXT_PUBLIC_API_URL=https://apife.factico.net

# Proxy /v1 → Edge Function (solo servidor; no NEXT_PUBLIC)
SUPABASE_FUNCTIONS_URL=https://rabcundgpzfzwnntjuxu.supabase.co/functions/v1/api
SUPABASE_ANON_KEY=pegar-anon-key-mismo-valor-que-arriba
